{
  "buildCommand": "SKIP_ENV_VALIDATION=true npm run build:vercel",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "env": {
    "SKIP_ENV_VALIDATION": "true",
    "NODE_ENV": "production"
  }
}