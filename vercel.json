{
  "buildCommand": "pnpm turbo run build --filter=@saasfly/nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/nextjs/.next",
  "github": {
    "silent": true
  }
}
