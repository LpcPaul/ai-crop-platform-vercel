{
  "buildCommand": "cd apps/nextjs && SKIP_ENV_VALIDATION=true pnpm build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "apps/nextjs/.next",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD",
  "github": {
    "silent": true
  }
}
